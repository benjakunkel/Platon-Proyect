<h1>Products#show</h1>
<p>Find me in app/views/products/show.html.erb</p>
<%= @product.name %>
<%= @product.description %>
<%= @product.previous_price %>
<%= @product.unit_price %>


<% if current_user.id == @shop.user_id || current_user.role == "admin" %>
<%= link_to "Editar producto", edit_product_path(@product)%>
<br>
<%= link_to "ELIMINAR producto", shop_product_path(@product), method: :delete %>
<% end %>